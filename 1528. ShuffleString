class Solution {
    public String restoreString(String s, int[] indices) {
        char curr[] = new char[s.length()];
        for(int i = 0; i < indices.length; i++){
            curr[indices[i]] = s.charAt(i);
        }
        return new String(curr);
    }
}
