class Solution {
    public boolean isAcronym(List<String> words, String s) {
        String newStr = "";
        for(int i = 0; i < words.size(); i++){
            newStr+=words.get(i).charAt(0);
        }
        if(newStr.equals(s)){
            return true;
        }
        return false;
    }
}
