class Solution {
    public int[] runningSum(int[] nums) {
        int sum = 0; 
        int size = nums.length;
        int arr[] = new int[size];
        for(int i = 0; i < arr.length; i++){
            sum += nums[i];
            arr[i] = sum;
        }
        return arr;
    }
}
