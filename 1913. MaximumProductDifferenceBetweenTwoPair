class Solution {
    public int maxProductDifference(int[] nums) {
        Arrays.sort(nums);
        int size = nums.length - 1;
        int result = (nums[size] * nums[size - 1]) - (nums[0] * nums[1]);
        return result;
    }
}
